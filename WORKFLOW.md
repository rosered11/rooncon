# วิธีทำงานกับ Claude Code ในโปรเจกต์นี้

## ⚠️ กฎข้อแรก — ต้องรัน `claude` จากในโฟลเดอร์นี้เท่านั้น

Claude Code อ่าน `CLAUDE.md` · skill · agent · memory จาก **โฟลเดอร์ที่คุณรันคำสั่ง** ไม่ใช่จากที่อื่น

```bash
cd ~/Documents/workspace/rooncon
claude
```

ถ้ารันจาก `~/Documents/project-youtube` จะไม่ได้อะไรเลย — ไม่มี skill ไม่มีความจำ เหมือนคุยกับคนที่ไม่เคยรู้จักช่องนี้

**ไฟล์งานของแต่ละคลิปให้วางใน `work/`** โฟลเดอร์นี้ถูก gitignore ไว้แล้ว ไฟล์ใหญ่ ๆ ไม่ขึ้น git แต่ Claude เข้าถึงได้

```
rooncon/
  .claude/          skill · agent · memory  ← commit
  CLAUDE.md                                  ← commit
  work/
    001-accidents/  ไฟล์คลิปนี้ทั้งหมด       ← ไม่ commit
    002-xxx/
```

---

## ครั้งแรก — ตั้งค่า 3 นาที

```bash
brew install ffmpeg tesseract     # ถ้ายังไม่มี
cd ~/Documents/workspace/rooncon
claude
```

ในหน้าต่าง Claude Code พิมพ์ตรวจสอบ:

| พิมพ์ | ควรเห็น |
|---|---|
| `/agents` | video-content · video-audio · video-timecode · video-imageqc |
| `/memory` | เห็น `CLAUDE.md` ของโปรเจกต์ **ไม่ใช่** ของ user |
| `มี skill อะไรบ้างในโปรเจกต์นี้` | 4 ตัว |

ถ้า `/agents` ว่าง แปลว่ารันผิดโฟลเดอร์

---

## รอบการทำคลิปหนึ่งคลิป

### 1 · หาหัวข้อ

```
หาหัวข้อคลิปถัดไปให้หน่อย
```

ได้ 5 หัวข้อพร้อมคะแนน เลือกหนึ่งแล้วสั่งต่อ:

```
เอาหัวข้อที่ 2 ทำ outline ให้ละเอียด
```

**ตรวจ outline ก่อนไปต่อ** ถ้าโครงไม่แน่น บทที่เขียนมาก็ไม่แน่น และการแก้ตอนนั้นแพงกว่ามาก

### 2 · เขียนบท

```
เขียนบทเต็มจาก outline นี้ ยาวประมาณ 9 นาที
เซฟที่ work/002-xxx/script.txt
```

ตรวจว่า: บุรุษที่ 2 ตลอด · ตัวเลขเป็นคำอ่านไทย · มีจุดพลิกชัด

### 3 · เสียง (ทำเองใน ElevenLabs)

v3 · Natural · export WAV แล้ววางที่ `work/002-xxx/raw.wav`

### 4 · มาสเตอร์เสียง

```
มาสเตอร์เสียง work/002-xxx/raw.wav ให้หน่อย
```

รอผลตาราง — ต้องได้ **-14 ถึง -16 LUFS · LRA 2–3 · clipped 0** ถ้าไม่ผ่านอย่าไปต่อ

### 5 · ถอด SRT (ทำเองใน SayToWords)

อัปไฟล์ที่มาสเตอร์แล้ว วางผลที่ `work/002-xxx/asr.srt`

### 6 · Timecode Map + ซับ

```
ทำ timecode map จาก
  เสียง  work/002-xxx/master.wav
  SRT    work/002-xxx/asr.srt
  บท     work/002-xxx/script.txt
```

ได้ Timecode Map · shot list · ซับสะอาด · จุด mid-roll

### 7 · gen ภาพ (ทำเองใน Google Flow)

วาง shot list ทีละ 40–50 ช็อต พร้อม Style Bible ทุก batch
โหลดมาไว้ที่ `work/002-xxx/download/`

### 8 · QC ภาพ

```
ตรวจภาพใน work/002-xxx/download/ เทียบกับ shot list
```

ได้รายงานแยกสามกลุ่ม — ผ่าน / แก้ให้แล้ว / ต้อง gen ใหม่

### 9 · ตัดต่อ (ทำเองใน CapCut)

ลากภาพเรียงตามชื่อไฟล์ + เสียงที่มาสเตอร์แล้ว
**ห้ามกด Normalize Loudness · ปิด Noise reduction · ปิด Voice enhance**

### 10 · metadata + อัปโหลด

```
ทำ metadata ให้คลิปนี้ — ชื่อ 5 แบบ description chapters tags thumbnail
```

---

## คำสั่งที่ใช้บ่อย

| อยากได้อะไร | พิมพ์ |
|---|---|
| ให้ agent ทำแยก ไม่กินบทสนทนาหลัก | `ใช้ video-imageqc ตรวจ work/002/download/` |
| ดู agent ทั้งหมด | `/agents` |
| ดูว่าความจำโหลดจากไหน | `/memory` |
| ล้างบทสนทนาแต่เก็บ context โปรเจกต์ | `/clear` |
| ย่อบทสนทนาที่ยาวมาก | `/compact` |
| ให้จำอะไรถาวร | `จำไว้ว่า ... เขียนลง .claude/memory/` |

---

## เมื่อไหร่ควรเรียก agent แยก

เรียกแบบธรรมดา (`ตรวจภาพให้หน่อย`) → ทำงานในบทสนทนาเดิม เห็นทุกอย่างที่คุยมา **ใช้เป็นค่าเริ่มต้น**

เรียก agent (`ใช้ video-imageqc ...`) → มี context แยก ผลลัพธ์กลับมาเป็นสรุป

ใช้ agent เมื่องานจะกิน context เยอะจนบทสนทนาเต็ม เช่นตรวจภาพ 221 ใบ หรือค้นเว็บหลายสิบหน้า

**agent ไม่เห็นสิ่งที่คุยมาก่อนหน้า** ต้องบอกพาธไฟล์ให้ครบทุกครั้ง

---

## วินัยเรื่องความจำ — ข้อเดียวที่ห้ามลืม

ทุกครั้งที่ค้นพบอะไรใหม่ ให้สั่ง:

```
จำไว้ว่า <สิ่งที่ค้นพบ> เขียนลง .claude/memory/
```

แล้ว commit:

```bash
git add .claude/memory && git commit -m "memory: ..." && git push
```

**ถ้าไม่ commit ความจำจะอยู่แค่ในเครื่องคุณ** ทีมจะไม่ได้ประโยชน์ และหายเมื่อเปลี่ยนเครื่อง

ถ้า Claude เสนอจะเขียนลง `~/.claude/` ให้ปฏิเสธและบอกให้เขียนลงโปรเจกต์แทน — `.claude/settings.json` บล็อกไว้แล้วชั้นหนึ่ง แต่ระวังเองอีกชั้นดีกว่า

---

## ต่างจาก Cowork ตรงไหน

| | Cowork | Claude Code |
|---|---|---|
| ไฟล์ | มีการ์ดให้กดเปิด | เซฟลงดิสก์เฉย ๆ ต้องเปิดเอง |
| การอ้างไฟล์ | ลากใส่ได้ | พิมพ์พาธ หรือ `@work/002/script.txt` |
| ความจำ | ผูกกับบัญชี | ผูกกับ repo — ทีมแชร์กันได้ |
| git | ไม่มี | ใช้ได้เต็มที่ |
| รันคำสั่ง | sandbox แยก | เครื่องคุณจริง — เห็น ffmpeg CapCut ทุกอย่าง |

ข้อได้เปรียบใหญ่ที่สุดของ Claude Code สำหรับงานนี้คือ **มันรัน ffmpeg บนเครื่องคุณได้ตรง ๆ** และ **แก้ draft CapCut ได้จริง** โดยไม่ต้องคัดลอกไฟล์ไปมา
