# Timecode และจังหวะ

## กฎเหล็ก

**เวลาเอาจาก SRT · ตัวหนังสือเอาจากสคริปต์ต้นฉบับ**

ASR ไทยอ่านผิดเสมอ ตัวอย่างจริง:

| ควรเป็น | ASR ได้ |
|---|---|
| เศษกระจก | เสกกระจก |
| ทุ่งหญ้า | ทุ่งย่า |
| ขวางเลน | ขวางเล่น |
| สิ่งกีดขวาง | สิ่งกิดขวาง |
| นักประสาทวิทยา | นับภาษาวิทยา |
| ความเสือก | ความเสื่อ |

## ความยาวช็อต

**2.5–2.8 วินาทีต่อภาพ** · จำนวนช็อต = ความยาวคลิป ÷ 2.6
95% ของเฟรมเป็นภาพนิ่ง ตัดแข็ง ไม่ต้องทำอนิเมชัน

**ที่มา:** วัดจากคลิปอ้างอิงในโฟลเดอร์ example
**วันที่:** 2026-08-06

## บทเรียน: ห้ามแบ่งเวลาตามสัดส่วนตัวอักษร

v4 ใช้สัดส่วนตัวอักษร → ภาพเลื่อนจากเสียงถึง **5.1 วินาที** ในช่วงกลางคลิป
เพราะคนพูดไม่ได้เร็วคงที่ และ ASR ตกคำไม่สม่ำเสมอ

v5 เปลี่ยนเป็น anchor — หาข้อความตรงกันเป๊ะ 12 ตัวอักษร ได้ 851 จุด + ผูกช็อตกับย่อหน้าด้วยมืออีก 61 จุด
ผล: คลาดเหลือ 0.04–1.5 วินาที

**วันที่:** 2026-08-06

## ชื่อไฟล์ภาพ = เวลา

`HH_MM_SS[_5].jpeg` เช่น `00_02_18_5.jpeg` = ปรากฏที่ 2:18.5
ลากเข้า timeline แล้วเรียงถูกเองตามชื่อ

## เกลี่ยความยาว

MIN 1.6 · MAX 4.2 วินาที · วนปรับ 400 รอบโดยไม่ขยับ anchor

## กรณีสองช่วงเวลาที่ติดกันบังคับให้ต้องใช้ภาพองค์ประกอบเดียวกันจริง ๆ — รวมช็อตแทนที่จะ gen ภาพซ้ำหน้าตา

ตามบทเรียนใน `images.md` (หัวข้อ "ทุกช็อตที่ติดกันควรต่างองค์ประกอบภาพกัน") — ปกติต้องสลับมุมกล้อง/ท่าทาง/การจัดวางทุกคู่ช็อตที่ติดกัน แต่ถ้าเนื้อหาบังคับจริง ๆ ว่าสองช่วงเวลานั้นต้องเป็นภาพหน้าตาเดียวกันเป๊ะ (ไม่มีทางทำให้ต่างกันได้อย่างสมเหตุสมผล) **ให้รวมสองช็อตเป็นช็อตเดียว ขยายเวลาแสดงผลของภาพเดิมแทนการ gen ภาพใหม่ที่หน้าตาเหมือนเดิม**

**เพดานความยาวช็อตที่ขยาย: ไม่เกิน 4 วินาที** (อยู่ในกรอบ MAX 4.2 วิ ของกฎเกลี่ยความยาวด้านบนอยู่แล้ว ไม่ใช่ข้อยกเว้นใหม่ที่ขัดกัน) ถ้ารวมแล้วยาวเกิน 4 วิ ให้กลับไปหาวิธีทำให้สององค์ประกอบต่างกันแทน อย่าปล่อยช็อตเดียวยาวเกิน 4 วิเพื่อเลี่ยงการ gen ภาพใหม่

**วันที่:** 2026-08-09

## Ken Burns

คลิปนิ่งเกินไปแม้ตัวเลขจะตรงเกณฑ์ทุกข้อ แก้ด้วยการซูมช้าในแต่ละช็อต
ช็อตยาวเกิน 3.5 วินาที ใส่ snap zoom ที่ประมาณ 52% ของช็อต (1.00 → 1.18) อ่านเหมือนมีคัต

**วันที่:** 2026-08-07

### บทเรียน: เกณฑ์ ">3.5 วิ" อย่างเดียวใช้ไม่ได้กับคลิปที่ช็อตสั้น — เปลี่ยนเป็นใส่ตามจังหวะแทน

คลิป 003 ทุกช็อตสั้นกว่า 3.5 วิหมด (ยาวสุด 3.3 วิที่ความเร็วธรรมชาติ, 3.0 วิหลังเร่ง 1.10x) — ผลคือ **ไม่มี Ken Burns เลยทั้งคลิป 213 ช็อต** ทั้งที่กฎเดิมทำตามถูกทุกตัวอักษร (ยืนยันคำนวณด้วยและเปิด `draft_info.json` ดูตรงว่าไม่มี `common_keyframes` ซูมสักช็อตเดียว) — เกณฑ์ duration ล้วนๆ จึงมีช่องโหว่กับคลิปที่ช็อตสั้นเป็นพิเศษ ขัดกับเจตนาเดิมของกฎ (กันคลิปนิ่งเกินไป)

**กฎใหม่ (ตัดสินใจแล้ว 2026-08-09):** ใส่ snap zoom แบบ**จังหวะ (rhythm) เป็นเกณฑ์หลัก ไม่ผูกกับความยาวช็อต**
- เลือกช็อตให้ซูมประมาณ**ทุก 8–12 ช็อต** สุ่มช่วงห่างไม่ตายตัวในกรอบนี้ (กันดูเป็นแพทเทิร์นตายตัวที่เดาจังหวะได้ล่วงหน้า — คนละปัญหากับที่เจอตอนช็อตซ้ำติดกัน แต่หลักการเดียวกันคือห้ามคาดเดาได้)
- ความถี่นี้ที่ความยาวคลิปทั่วไป (~500 วิ, ช็อตเฉลี่ย 2.4–2.6 วิหลังเร่งความเร็ว) จะได้ซูมประมาณ **ทุก 20–25 วินาที** — ผู้ใช้ยืนยันว่าความถี่นี้ "ไม่บ่อยจนปวดตา"
- เอฟเฟกต์ซูมยังใช้ค่าเดิม: `1.00 → 1.18` snap ที่ตำแหน่ง ~52% ของช็อตนั้น (ดู schema จริงที่หัวข้อ "Ken Burns เก็บใน `segment.common_keyframes`" ด้านล่าง)
- ช็อตที่ยาวเกิน 3.5 วิเองอยู่แล้ว (คลิปอื่นที่ช็อตยาวกว่านี้) ยังใส่ซูมได้ตามเดิมด้วย ไม่ต้องกันออกจากการนับจังหวะ — สองเงื่อนไขไม่ขัดกัน แค่จังหวะเป็นตัวเสริมให้คลิปช็อตสั้นก็ยังมีซูมบ้าง

**วันที่:** 2026-08-09

## CapCut

CapCut 9 อ่าน `Timelines/<uuid>/draft_info.json` เป็นตัวจริง ไฟล์ `draft_info.json` ที่ราก draft เป็นของเก่าที่ถูกละเลย
แก้ผิดไฟล์แล้วเปิดมาจะเหมือนไม่มีอะไรเปลี่ยน — เขียนทับทุกไฟล์ที่เป็นไปได้

## บทเรียน: ห้ามแก้ไฟล์ draft ขณะ CapCut เปิดโปรเจกต์นั้นค้างอยู่

แก้ `draft_info.json` ตรง ๆ ด้วยสคริปต์ตอนที่ CapCut เปิดโปรเจกต์นั้นอยู่ (เห็นใน `ps aux` ว่า process ยังรัน) — พอโปรแกรม autosave มันเขียนทับไฟล์ด้วยข้อมูลในหน่วยความจำของมันเอง (ที่ไม่มีการแก้ไขที่เพิ่งทำ) ทับทุกอย่างที่เพิ่งเขียนไปจนหายสนิท เจอกับ track BGM ที่เพิ่งเพิ่มหายไปทั้ง track
**กฎจากนี้ไป:** ก่อนแก้ `draft_info.json` ด้วยสคริปต์ทุกครั้ง **ต้องให้ผู้ใช้ปิด CapCut ให้สนิทก่อน** (เช็คด้วย `ps aux | grep -i capcut` ว่าไม่มี process เหลือ) ห้ามเปิดแอปอีกจนกว่าจะยืนยันว่าแก้เสร็จแล้ว

**วันที่:** 2026-08-08

## บทเรียน: สคริปต์ .txt ที่ Read แสดงเลขบรรทัด ≠ เลขในไฟล์จริง

Read tool แสดงผลแบบ `cat -n` (เติมเลขบรรทัดเอง) ห้ามเข้าใจผิดว่าไฟล์สคริปต์มีเลขย่อหน้ากำกับอยู่จริง
ต้องเช็คด้วย `od -c` หรือ `cat -A` ก่อนเขียนโค้ดพาร์สที่สมมติรูปแบบ `เลข\tข้อความ`

**วันที่:** 2026-08-08

## บทเรียน: ใช้ pythainlp ตัดคำช่วยแบ่งช็อตไม่ให้ขาดกลางคำ

แบ่งช็อตด้วยจำนวนตัวอักษรล้วน ๆ (แม้จะยึดเวลาแบบ anchor ถูกแล้ว) ยังเสี่ยงตัดกลางคำผสม เช่น "หลับ" โดนตัดเป็น "หลั" + "บ"
วิธีแก้: ใช้ `pythainlp.tokenize.word_tokenize` (engine `newmm`) ตัดเฉพาะประโยค/วลีที่ยาวเกิน MAXTOK (~30 ตัวอักษร) ก่อนค่อยจัดกลุ่มเป็นช็อต ทำให้จุดตัดตกที่ขอบคำเสมอ

**ต้องรวมเลขที่สะกดเป็นคำ (ปี ค.ศ., เปอร์เซ็นต์) เป็นก้อนเดียวก่อนตัด** ไม่งั้น tokenizer จะตัด "หนึ่งพันเก้าร้อยหกสิบหก" ออกเป็น 6 ชิ้นแยกกัน ทำให้ปี 1966 ไปอยู่คนละช็อตกับตัวเลขท่อนหลัง — merge token ที่เป็นคำจำนวนไทยติดกันให้เป็นก้อนเดียวก่อนแบ่งช็อต

**วันที่:** 2026-08-08

## บทเรียน: ตัดซับ (.srt) ต้อง snap ไปจุดช่องว่างจริง ห้ามใช้ตำแหน่งตัวอักษรดิบ

ถ้าคำนวณจุดตัดบรรทัดซับจากตำแหน่งตัวอักษรที่ interpolate มาตรง ๆ (ไม่ปัดไปจุดช่องว่างของคำ) จะได้ซับขาดกลางคำ เช่น "ทั้งที่" โดนตัดเป็น "ท" กับ "ั้งที่"
ต้องสร้างชุดตำแหน่ง "ขอบคำจริง" จากช่องว่างในสคริปต์ต้นฉบับไว้ก่อน แล้ว snap จุดตัดทุกจุดไปตำแหน่งขอบคำที่ใกล้ที่สุดเสมอ — ตรวจสอบท้ายสุดด้วยการต่อข้อความซับทั้งหมดกลับมาเทียบกับสคริปต์เต็ม ต้องได้ตัวอักษรตรงกัน 100%

**วันที่:** 2026-08-08

## บทเรียน: หา mid-roll จากช่วงเงียบจริงในคลื่นเสียง ไม่ใช่กะจากข้อความ

ช่องว่างระหว่างประโยคใน SRT (จาก SayToWords) มักถูกเครื่องมือดันให้ชนกันจนไม่เห็นช่องว่าง ต้องรัน `ffmpeg silencedetect` ตรงกับไฟล์เสียงจริงเพื่อหาจุดเงียบ (ลอง noise=-30dB ถึง -35dB, d=0.15–0.5) แล้วจับคู่กับจุดในสคริปต์ที่เปิดช่องว่างความอยากรู้ (ก่อนคำเฉลย/ก่อน "แต่...")

**วันที่:** 2026-08-08

## บทเรียน: naive "find(w, last)" ทำ anchor หาย 90%+ เมื่อบทมีตัวเลขสะกดต่างจาก ASR เยอะ

คลิป 003 (คอยีราฟ) สคริปต์สะกดตัวเลขเป็นคำไทยเสมอ ("เจ็ดชิ้น", "สองร้อยล้านปี") แต่ SRT จาก ASR เขียนเป็นเลขอารบิก ("7 ชิ้น", "200 ล้านปี") — วิธี `srt_ns.find(w, last)` แบบ window 12 ตัวอักษรใน skill ได้ anchor แค่ **72 จุด** จากที่ควรได้หลักร้อย เพราะตัวเลขที่สะกดต่างกันทำให้ไม่มี substring ตรงกันเป๊ะยาว 12 ตัวอักษรในช่วงนั้นเลย

**ทางแก้ที่ได้ผล (94%+ ของสคริปต์จับคู่ได้):**
1. **normalize ตัวเลข**: scan SRT หาเลขอารบิก แปลงเป็นคำไทยด้วย `pythainlp.util.num_to_thaiword(n)` ก่อนจับคู่ (ยังคง track ตำแหน่งเดิมใน SRT ไว้คู่กันเพื่อคำนวณเวลา)
2. **ใช้ diff-match-patch หรือ difflib.SequenceMatcher แทน naive find** — ได้ matching blocks ที่ไม่หลุดหายเวลาข้อความมี insert/delete เล็ก ๆ (ASR เพิ่ม/ลดคำ) ทั้งสองไลบรารีให้ผลเหมือนกันในเคสนี้ (94.3% matched)
3. **จุดพลาดที่ทำให้เสียเวลาไปมาก:** สร้าง anchor point แค่ที่ "จุดเริ่ม-จุดจบของแต่ละ matched block" (2 จุดต่อ block) — พอ block ยาว (500+ ตัวอักษร ครอบคลุมหลาย SRT segment) จะกลายเป็น linear interpolation ข้ามช่วงเวลายาวเหมือนวิธีสัดส่วนที่ห้ามใช้ **ต้องเพิ่ม anchor point ที่ทุกจุดที่ตำแหน่งใน SRT ข้าม segment boundary ภายใน block เดียวกันด้วย** ไม่ใช่แค่หัวท้าย block — แก้แล้วได้ 380 anchor points, gap สูงสุดเหลือ 72 ตัวอักษร (~5 วิ) จากเดิม 525 ตัวอักษร (~40 วิ)

**ตรวจสอบว่าแม่นจริง:** สุ่มชื่อเฉพาะ 5 จุด (ชื่อนักวิจัย, ตัวเลขจุดพลิก) เทียบตำแหน่งคำจริงใน SRT (ไม่ใช่จุดเริ่ม segment) ได้คลาด 0.00–0.02 วินาทีทุกจุด — **ระวังอย่าเทียบกับเวลาเริ่มของทั้ง SRT segment เพราะคำที่ต้องการอาจอยู่กลาง/ท้าย segment ทำให้ดูเหมือนคลาดเยอะทั้งที่จริงแม่น**

**วันที่:** 2026-08-09

## บทเรียน: pythainlp tokenizer สับคำเฉพาะ/ชื่อคนที่ไม่อยู่ในดิกชันนารีจนขาดกลางคำ

`word_tokenize(engine='newmm')` เจอชื่อคนที่ทับศัพท์ (เช่น "ฟรีตสัน กาลิส", "แมทธิว วีเดล") จะตัดเป็นพยางค์มั่ว ๆ เช่น `['ฟรี','ต','สัน']` และ `['กา','ลิ','ส']` — ถ้าเอาจุดตัดพวกนี้ไปแบ่งช็อต/ซับ จะได้ภาพหรือซับที่ตัดกลางชื่อคน (เช่น "...ชื่อฟรี" / "ตสัน กาลิส...")

**ทางแก้:** เก็บรายชื่อเฉพาะ/คำทับศัพท์ที่ปรากฏในสคริปต์ไว้ก่อน (ชื่อคน, ศัพท์เทคนิคทับศัพท์อย่าง "เมแทบอลิซึม") แล้วสร้าง custom dict ผ่าน `pythainlp.tokenize.Tokenizer(custom_dict=set(thai_words()) | {'ฟรีตสัน','กาลิส','แมทธิว','วีเดล','เมแทบอลิซึม', ...}, engine='newmm')` — ต้องเช็คทุกครั้งที่ขึ้นคลิปใหม่ว่ามีชื่อเฉพาะ/ศัพท์ทับศัพท์อะไรบ้างในสคริปต์ แล้วเพิ่มเข้า custom dict ก่อน tokenize เสมอ ไม่ใช่เชื่อ dictionary เริ่มต้นเฉย ๆ

**วิธีเช็คว่ายังมีคำหลุดเหลือ:** หลัง tokenize ให้กรอง token สั้น (≤3 ตัวอักษร) ที่ไม่ใช่คำไทยทั่วไป (กรองด้วย set คำสั้นที่พบบ่อยไว้) มาดูด้วยตาว่าเป็นเศษพยางค์ของคำอะไรหรือเปล่า

**วันที่:** 2026-08-09

## CapCut — schema เพิ่มเติมที่เรียนรู้จากคลิป 003 (giraffe neck)

เรียนรู้จากการโคลน `002_first_night_sleep` → `003_giraffe_neck` (jq เจาะ `draft_info.json` ของแม่แบบ)

### root `draft_info.json` กับ `Timelines/<UUID>/draft_info.json` เหมือนกันทุกไบต์
`diff <(jq -S . root) <(jq -S . timeline)` ได้ผลว่างเปล่าใน 002 — สองไฟล์นี้ควรเป็นก้อนเดียวกันเป๊ะเสมอ เขียนทับด้วย object เดียวกันได้เลย ไม่ต้องคำนวณแยก

### รูปภาพนิ่ง (photo) ไม่มี "speed" ของตัวเอง — ต้องหารเวลาด้วยมือ
`materials.videos[].type == "photo"` มี `duration` เป็นค่า placeholder ใหญ่คงที่ (`10800000000` = 3 ชม.) ไม่ใช่ความยาวจริง ความยาวจริงกำหนดจาก `segment.source_timerange` / `target_timerange` เท่านั้น (สองค่านี้เท่ากันเสมอสำหรับภาพนิ่ง) และ `segment.speed` ของภาพนิ่งคงที่ `1.0` เสมอแม้คลิปทั้งตัวจะ "เร่ง 1.10x" ก็ตาม

**การเร่งความเร็วคลิปทั้งตัว (เช่น 1.10x) ไม่ได้ทำผ่าน field "speed" ของ segment ภาพ** — วิธีจริงคือ **หารเวลา in/out ของทุกช็อต (จาก timecode_map ที่คำนวณไว้ที่ความเร็วธรรมชาติ) ด้วยตัวคูณความเร็วเอง** แล้วค่อยใส่เป็น target_timerange ของแต่ละ segment ภาพ (คำนวณแบบสะสม `target_start_i = target_end_{i-1}` กัน rounding drift สะสมจนเกิดช่องว่าง)

ส่วนไฟล์เสียงพากย์ (WAV จริง มีคลื่นเสียงจริง) ใช้กลไก speed จริงของ CapCut: `segment.speed = 1.1`, `source_timerange.duration` = ความยาวไฟล์ธรรมชาติจริง (นับ frame จาก `wave` module ไม่ใช่ ffprobe เพราะแม่นกว่า), `target_timerange.duration = round(source/1.1)`

ยืนยันรูปแบบนี้จากโปรเจกต์ทดลอง `draft004_110x` ที่มีอยู่ในเครื่อง (ไม่ใช่แม่แบบทางการแต่ช่วยยืนยัน pattern เดียวกัน)

### Ken Burns เก็บใน `segment.common_keyframes` ไม่ใช่ `materials.material_animations`
`materials.material_animations[]` (type `sticker_animation`) ที่ทุก segment อ้างถึงเป็นแค่ placeholder ว่างเปล่า (`animations: []`) เสมอ — **Ken Burns จริงอยู่ใน `segment.common_keyframes`**:
```json
"common_keyframes": [{
  "property_type": "KFTypeScaleX",
  "keyframe_list": [
    {"time_offset": 0, "values": [1.0]},
    {"time_offset": <52% ของ target_timerange.duration>, "values": [1.0]},
    {"time_offset": <52%+100000 (0.1s)>, "values": [1.18]}
  ]
}]
```
มีแค่ `KFTypeScaleX` เท่านั้น ไม่มี `KFTypeScaleY` คู่กัน (แกน Y ตามแกน X เองผ่าน `uniform_scale.on: true` ของ segment) — เกณฑ์ >3.5 วิที่ทีมตั้งไว้ ใช้กับ **target_timerange.duration หลังเร่งความเร็วแล้ว** (ไม่ใช่ความยาวธรรมชาติก่อนเร่ง) เพราะเป็นความยาวที่คนดูเห็นจริงบนไทม์ไลน์ — คลิป 003 รอบแรกไม่มีช็อตไหนถึงเกณฑ์เลยแม้แต่ช็อตเดียว (max หลังเร่งคือ ~3.0 วิ) จึงไม่มี Ken Burns เลยทั้งคลิป **⚠️ อัปเดต 2026-08-09: เปลี่ยนกฎเป็นใส่ตามจังหวะทุก 8–12 ช็อตแทนแล้ว (ดูหัวข้อ "บทเรียน: เกณฑ์ >3.5 วิ อย่างเดียวใช้ไม่ได้กับคลิปที่ช็อตสั้น" ด้านบน) ต้องย้อนไปเติม `common_keyframes` ให้คลิป 003 เพิ่ม**

### segment แต่ละตัว (ภาพ/เสียง) ผูกกับ material ผ่าน `extra_material_refs` 6–8 ตัว เสมอ
ภาพ (8 refs): `speed, placeholder_info, canvas, material_animation, sound_channel_mapping, material_color, loudness, vocal_separation`
เสียง (6 refs): `speed, placeholder_info, beats, sound_channel_mapping, loudness, vocal_separation`
ทุก segment ต้องมีครบชุดแม้ค่าจะเป็น default ว่างเปล่า มิฉะนั้น CapCut อาจ error ตอนเปิด — `loudnesses[].enable` และ `vocal_separations[].choice` ต้องเป็น `false`/`0` เสมอ (คือค่า "ปิด" ที่กฎช่องต้องการอยู่แล้ว)

### BGM สอง segment มาจากไฟล์เดียวกัน ต่อเนื่อง ไม่ loop ซ้อน
`materials.audios` ของ BGM ทั้ง 2 segment (ระหว่างพากย์ 10% / end credit 40%) ชี้ไป **material_id เดียวกัน** ไฟล์เดียวกัน (`bgm.wav` ที่ loop+crossfade+trim มาแล้วนอก CapCut ด้วย ffmpeg ตามสูตรใน `decisions.md`) แค่ตัด `source_timerange.start` ให้ต่อกันพอดี (segment 2 เริ่มที่ `source_timerange.start` = จุดจบของ segment 1) — **fade in/out ของ BGM ไม่ได้ทำผ่าน CapCut (ไม่มี `audio_fades` material ใช้งานเลย, count เป็น 0 เสมอ)** ต้อง bake `afade` เข้าไปในไฟล์ wav ตอนเตรียมด้วย ffmpeg ก่อน import เท่านั้น

### ไม่มี text/subtitle track ใน draft ที่เสร็จสมบูรณ์
`002_first_night_sleep` ที่อัปโหลดจริงแล้วไม่มี track type `text` และ `materials.texts` มี 0 รายการ — ซับไม่ได้ถูก burn เข้า CapCut เลย แปลว่าซับถูกอัปโหลดแยกเป็นไฟล์ `.srt` ตรงตอน YouTube upload (ขั้น 10 ของ pipeline) ไม่ใช่ส่วนหนึ่งของงาน CapCut edit — ไม่ต้องพยายามสร้าง text track ใน draft_info.json

### ไม่มี mid-roll marker ใน `draft_info.json` เลย
เช็คทั้งไฟล์ (`time_marks`, `keyframe_graph_list`, grep "marker"/"ad_break"/"chapter") ไม่เจอกลไกฝัง mid-roll ใน CapCut project เลย — mid-roll ของช่องนี้ตั้งทีหลังตอนอัปโหลดใน YouTube Studio เท่านั้น ตำแหน่งที่คำนวณจาก `midroll_positions.md` (คำนวณที่ความเร็วธรรมชาติ) ต้องหารด้วยตัวคูณความเร็วเองก่อนไปตั้งใน YouTube Studio ถ้าคลิปเร่งความเร็ว

### ภาพต้นทาง 1376×768 อัปสเกลเป็น 1920×1080 ด้วย stretch ตรง ๆ ไม่ crop
เทียบ pixel MSE ระหว่างภาพต้นฉบับ resize แบบ stretch ตรงกับภาพ `final_upscaled/` ของ 002 จริง (MSE 0.68) กับแบบ scale-then-center-crop (MSE 92.7) — stretch ตรงคือวิธีที่ใช้จริง แม้ aspect ratio จะต่างกันเล็กน้อย (1376:768 = 1.7917 vs 1920:1080 = 1.7778) ก็ไม่ครอบตัด ใช้ `Image.resize((1920,1080), LANCZOS)` ตรง ๆ ได้เลย

### `draft_meta_info.json` มี `draft_fold_path`/`draft_name` ต้องอัปเดตด้วยตอนโคลนโปรเจกต์
ไฟล์นี้อยู่นอก `Timelines/` ที่รากโปรเจกต์ ใช้เก็บ path เดิมของโปรเจกต์ต้นแบบ (เช่น ยังชี้ไป `002_first_night_sleep` หลังโคลน) ต้องแก้ `draft_name`, `draft_fold_path`, `tm_draft_modified`, `tm_duration` ให้ตรงโปรเจกต์ใหม่ด้วย ไม่งั้น CapCut อาจสับสนเรื่อง path แม้ตัว `draft_info.json` จะถูกต้องแล้วก็ตาม — ไฟล์อื่น ๆ ที่ยังอ้างชื่อโปรเจกต์เดิม (`template.json`, `template-2.tmp`, `mini_draft.json`) ไม่ได้แก้เพราะไม่ใช่ไฟล์ที่ CapCut อ่านเป็นหลัก (อยู่นอกขอบเขตกฎเหล็กของ skill) — ยังไม่ยืนยันว่าจำเป็นต้องแก้ไหม รอดูตอนเปิด CapCut จริงครั้งแรกกับโปรเจกต์ที่โคลนมา
**`root_meta_info.json`** (อยู่นอกโฟลเดอร์โปรเจกต์ ระดับ `com.lveditor.draft/` เป็น index ของ CapCut ทั้งหมด) ยังไม่ถูกแก้เช่นกัน — ไม่แน่ใจว่า CapCut auto-scan โฟลเดอร์ใหม่ตอนเปิดแอปเองหรือไม่ ต้องรอดูผลตอนเปิดจริง ถ้าโปรเจกต์ใหม่ไม่ขึ้นในลิสต์ให้ลองใช้ "Import"/"Open" ชี้ path ตรง ๆ ก่อน แล้วค่อยพิจารณาแก้ root_meta_info.json (ระวังเพราะกระทบทุกโปรเจกต์ในเครื่อง)

**วันที่:** 2026-08-09

## บทเรียน: schema `extra_material_refs` ของ CapCut — ลำดับหมวดหมู่ตายตัวต่อ track type

จากงานเพิ่ม end credit ใน `003_giraffe_neck` เรียนรู้ว่า `extra_material_refs` ของแต่ละ segment คือ list ของ id ที่ชี้ไปยัง "material ปลีกย่อย" หลายหมวดใน `materials.*` (ค่า default/placeholder เกือบทั้งหมด ไม่มีเนื้อหาจริง) — **แต่ละ segment ต้องมีชุดของตัวเองแยกกัน ห้ามใช้ id ซ้ำข้าม segment** แม้ค่าข้างในจะเหมือนกันทุกตัวก็ตาม (โคลนแล้วสุ่ม id ใหม่เสมอ)

ลำดับหมวดที่เจอจริง (คงที่ตาม track type):

- **video segment** (8 refs): `speeds → placeholder_infos → canvases → material_animations → sound_channel_mappings → material_colors → loudnesses → vocal_separations`
- **audio segment** (6 refs, ทั้งเสียงพากย์และ BGM): `speeds → placeholder_infos → beats → sound_channel_mappings → loudnesses → vocal_separations`

วิธีปลอดภัยสุด: อย่าจำลำดับตายตัว ให้เขียนสคริปต์ที่ไล่หาหมวดหมู่จริงของแต่ละ ref id จาก segment ตัวอย่าง (loop ทุก key ใน `materials` ที่เป็น list แล้วเทียบ `id`) แล้วโคลนตามหมวดที่เจอจริงเสมอ ไม่ต้อง hardcode

## บทเรียน: ไม่มี `audio_fades` ใน JSON ของ track BGM — fade in/out ถูก bake เข้าไฟล์ wav เลย

ตรวจ `003_giraffe_neck` แล้วพบว่า BGM segment ไม่มี fade material อ้างอิงใน `extra_material_refs` เลย (ไม่มีหมวด audio_fades ถูกใช้งาน) เพราะ fade in 2 วิ/fade out 3 วิ ถูกทำด้วย ffmpeg (`afade`) ใส่ไว้ในไฟล์ `bgm.wav` เองตั้งแต่ก่อน import เข้า CapCut แล้ว — ไม่ต้องหา/ตั้งค่า fade ผ่าน JSON เลย

## บทเรียน: ต่อ BGM segment สองช่วง volume ต่างกันแบบไม่มีรอยตัด ทำได้ด้วยการแบ่ง segment ไม่ใช่ตัดไฟล์เสียง

เวลาต้องการให้ BGM ไฟล์เดียวกันมีความดังสองระดับ (เช่น 10% ระหว่างมีเสียงพากย์ → 40% ช่วง end credit ไม่มีเสียงพูดแข่ง) โดยไม่ให้มีรอยตัด/เริ่มเพลงใหม่ ให้:
1. เรนเดอร์ไฟล์เสียง BGM ยาวคลุมทั้งความยาว timeline สุดท้าย (main content + end credit) เป็นไฟล์เดียวต่อเนื่อง (ใช้เทคนิค loop+crossfade เดิม แต่ trim ยาวขึ้น และย้ายจุด fade out ไปไว้ที่ปลายไฟล์จริงแทน)
2. ใน `draft_info.json` สร้าง audio segment ใหม่อีกอันที่ชี้ `material_id` **เดียวกัน** กับ segment เดิม แต่ `source_timerange.start` ต่อจากจุดที่ segment แรกจบพอดี แล้วตั้ง `volume` ต่างกันได้อิสระต่อ segment
3. ผลคือเสียงเล่นต่อเนื่องไม่มีช่องว่าง/ไม่มีรอยตัดที่ได้ยิน แต่ความดังเปลี่ยนขั้นบันไดตรงจุดต่อ segment

**วันที่:** 2026-08-09

## บทเรียน: เติม Ken Burns ย้อนหลังให้คลิป 003 ตามกฎจังหวะ 8–12 ช็อต — วิธีระบุ "ช็อตหลัก" แยกจาก "การ์ด end credit" ใน JSON

ตอนเขียนสคริปต์เติม `common_keyframes` ย้อนหลังให้ `003_giraffe_neck` (213 ช็อตหลัก + 2 การ์ด end credit = 215 segment ใน video track เดียวกัน) พบวิธีแยกที่เชื่อถือได้: **การ์ด end credit มี `target_timerange.duration == 6000000` (6 วิเป๊ะ) เสมอ ส่วนช็อตหลักไม่มีช็อตไหนยาวเท่านี้พอดี** — ใช้เงื่อนไขนี้กรองแทนการนับ index ท้าย ๆ ตรง ๆ ปลอดภัยกว่าเพราะไม่ผูกกับลำดับ

**ผลการสุ่มจริงที่ใช้ (seed คงที่ `random.seed(20260809)` เพื่อ reproducible):** ได้ 21 ช็อตจาก 213 (index 1-based): 8, 19, 27, 35, 46, 57, 69, 77, 86, 96, 106, 115, 126, 134, 144, 153, 163, 175, 183, 192, 204 — gap ระหว่างจุดอยู่ในช่วง 8–12 ทุกจุด ตรงตามกฎจังหวะที่ตั้งไว้

**ยืนยันเพิ่ม:** `root draft_info.json` และ `Timelines/<UUID>/draft_info.json` ของ `003_giraffe_neck` มี segment id/order ตรงกัน 100% (diff เปล่า) ก่อนแก้ — เขียนสคริปต์แก้ทั้งสองไฟล์แยกรอบด้วย logic เดียวกัน (ส่ง random indices ชุดเดียวกันไปทั้งคู่ผ่านตัวแปรกลาง ไม่สุ่มซ้ำสองรอบ) เพื่อการันตีว่าช็อตที่ได้ keyframe ตรงกันทั้งสองไฟล์เป๊ะ — ตรวจ post-hoc ด้วย `diff` ของ list id ที่มี `common_keyframes` ไม่ว่างระหว่างสองไฟล์ ผ่าน (MATCH)

**วันที่:** 2026-08-09

## บทเรียน: แก้ speed เฉพาะ segment เดียว (ไม่แตะ track อื่น) ต้องเช็คว่า speed material ถูกใช้ร่วมกับ segment อื่นหรือไม่ก่อนแก้

เคสจริง: ทดสอบเปลี่ยนเสียงพากย์ของ `003_giraffe_neck` จาก `speed=1.1` กลับเป็น `1.0` ชั่วคราวเพื่อฟังเทียบว่าเสียงแตก/เพี้ยนมาจากอัลกอริทึมเร่งความเร็วของ CapCut หรือไม่ — งานแบบนี้ต้องแก้ **แค่ segment เดียว** โดยไม่กระทบ segment อื่นที่อาจ share material เดียวกัน

**วิธีระบุ material ที่ปลอดภัยจะแก้ (ไม่ใช่เดา):**
1. หา segment เป้าหมายจาก `material_id` ที่ชี้ไปไฟล์เสียงจริง (เช่น `ElevenLabs_003_mastered.wav`) ผ่าน `.tracks[].segments[] | select(.material_id==...)`
2. ในบรรดา `extra_material_refs` ของ segment นั้น หาตัวที่อยู่ใน `materials.speeds[]` โดยกรองด้วยค่า `speed` ที่ตรงกับ `segment.speed` ปัจจุบัน (เช่น `speed==1.1`) แทนการเดา index/ตำแหน่งในลิสต์ — `materials.speeds[]` มีหลายสิบรายการ ส่วนใหญ่เป็น `speed:1.0` (default ของ segment ภาพ) มีแค่ตัวเดียวที่ตรงกับ 1.1 พอดีในเคสนี้
3. **ก่อนแก้ ต้องเช็คว่า speed material id นั้นถูกอ้างโดย segment อื่นด้วยหรือไม่**: `jq --arg id "<speed_material_id>" '[.tracks[].segments[] | select(.extra_material_refs[]? == $id)]'` — ถ้าเจอมากกว่า 1 segment ห้ามแก้ material ร่วมตรง ๆ (จะกระทบ segment อื่นโดยไม่ตั้งใจ) ต้อง clone material ใหม่แยกให้ segment เป้าหมายอ้างอิงเอง แล้วค่อยแก้ตัวโคลน — เคสนี้เช็คแล้วพบว่า material ผูกกับ segment เดียว (1:1) จึงแก้ตรงได้เลยโดยปลอดภัย
4. เปลี่ยน `segment.speed` ให้ตรงกับ material ที่แก้ไปด้วยเสมอ (สอง field ต้องซิงค์กัน — CapCut เก็บ speed ซ้ำสองที่: ใน segment เองและใน material speeds ที่ extra_material_refs ชี้ไป)
5. เมื่อ speed=1.0 ความยาว `target_timerange.duration` ต้องเท่ากับ `source_timerange.duration` เต็มไฟล์เสมอ (ไม่มีการเร่ง/ผ่อนความเร็วแล้ว)
6. ตรวจสอบผลด้วย `diff <(jq -S . ก่อนแก้) <(jq -S . หลังแก้)` ต้องเห็นแค่บรรทัดที่ตั้งใจแก้เท่านั้น (เคสนี้คือ 3 บรรทัดเป๊ะ: speed ในสอง object + target duration 1 ค่า) เป็นวิธี sanity check ที่แม่นกว่าการอ่านด้วยตา

**หมายเหตุ:** top-level `duration` ของ project (`data["duration"]` ที่ root ของ draft_info.json) **ไม่ได้ปรับตามความยาว track เสียงที่ยาวขึ้นอัตโนมัติ** — เคสทดสอบนี้ตั้งใจปล่อยไว้ตามเดิม (502.42s) ในขณะที่เสียงพากย์ยาวจริง 552.63s เพราะเป็นการทดสอบฟังเสียงชั่วคราว ไม่ใช่ค่าสุดท้าย — **ยังไม่ยืนยันว่า CapCut จะเล่นเสียงเกินขอบ project duration เดิมได้เต็มหรือไม่ ต้องรอดูตอนเปิดแอปจริง** ถ้าจะใช้ค่านี้เป็นค่าจริงถาวร (ไม่ใช่แค่ทดสอบ) ต้องอัปเดต top-level `duration` ให้ตรงกับ track ที่ยาวที่สุดด้วย

**วันที่:** 2026-08-10

## บทเรียน: เสียงแตกจาก CapCut speed engine เรียลไทม์ — แก้ถาวรด้วยการเร่งไฟล์นอก CapCut แล้ว import แทน speed field

คลิป 003 (giraffe-neck) เจอเสียงแตกตอนตั้ง `segment.speed = 1.1` ให้ CapCut เร่งความเร็วเสียงพากย์เรียลไทม์เอง — ทดสอบ `speed = 1.0` แล้วเสียงสะอาด ยืนยันว่าเสียงแตกมาจาก speed engine ของ CapCut เอง ไม่ใช่ปัญหาไฟล์ต้นทาง

**ทางแก้ถาวร:** เร่งความเร็วไฟล์เสียงพากย์มาสเตอร์แล้วด้วย `ffmpeg atempo=1.1` นอก CapCut ก่อน (ได้ไฟล์ `*_110x.wav`) แล้ว import เป็น material ใหม่ ตั้ง `segment.speed = 1.0` (ไม่เร่งซ้ำ) — `source_timerange.duration` และ `target_timerange.duration` ของ segment ต้องเท่ากับความยาวไฟล์ 110x จริง (วัดด้วย `wave` module ไม่ใช่ ffprobe ตามบทเรียนเดิม)

**ขั้นตอนแก้ segment เสียงพากย์ให้ชี้ไฟล์ใหม่ (ยืนยันวิธีที่ปลอดภัย):**
1. หา segment id เดิม + material_id เดิมที่ segment ชี้ไป
2. เช็คว่า material นั้น (และ speed material ใน `extra_material_refs`) ถูกใช้กับ segment เดียวนี้เท่านั้น (นับจำนวน segment ที่อ้างถึงแต่ละ id) ก่อนแก้ กันกระทบ segment อื่นที่ share material เดียวกันโดยไม่รู้ตัว
3. **โคลน material entry เดิมเป็นก้อนใหม่** (deep copy) เปลี่ยนแค่ `id` (uuid4 ใหม่), `unique_id`, `local_material_id`, `music_id`, `name`, `path`, `duration` — อย่าแก้ path ของ material เดิมตรง ๆ เพราะจะเสียของเดิมไว้ทดสอบย้อนกลับไม่ได้ถ้าจำเป็น
4. แก้ segment: `material_id` ชี้ไป material ใหม่, `speed` เป็น 1.0, `source_timerange.duration` และ `target_timerange.duration` เท่ากับ duration ของไฟล์ใหม่จริง
5. Speed material ใน `extra_material_refs` ตั้ง `speed: 1.0` (ถ้าเดิมเป็น 1.0 อยู่แล้วจากการทดสอบรอบก่อนไม่ต้องแตะ)

**วิธีตรวจสอบว่าแก้แค่จุดที่ตั้งใจ:** เทียบ segment id list ของทุก track ระหว่างไฟล์ก่อน/หลังแก้ (ต้อง identical ทั้ง type/count/order) แล้วไล่ diff เนื้อหา segment ทีละตัวด้วย id เดียวกัน — ถ้ามี segment อื่นเปลี่ยนไปโดยไม่ตั้งใจจะเห็นทันที (ในเคสนี้ยืนยันว่าเปลี่ยนแค่ segment เสียงพากย์ 1 ตัวจากทั้งหมด 218 segment ในโปรเจกต์)

**บั๊กที่ต้องระวังตอนเขียนสคริปต์ query ตรวจสอบ (ไม่ใช่บั๊กในไฟล์ draft):** เขียน loop ซ้อน `for track in tracks: if track['type']=='audio': for seg in track['segments']: if seg matches...` แล้วใช้ตัวแปร `seg` ต่อนอกลูปเพื่ออ้างอิง — ตัวแปรจะถูก overwrite เป็น segment ตัวสุดท้ายที่ loop ผ่าน (อาจเป็นคนละ track ที่ type ตรงกันแต่ id ไม่ตรง) ไม่ใช่ตัวที่ match เงื่อนไข ทำให้ debug หลงประเด็นไปเช็ค material ผิดตัว (เจอตอนเช็คว่า voice segment ชี้ไปไฟล์ไหน ดันไปได้ข้อมูล bgm.wav แทน) — **ต้องเก็บผลลัพธ์ที่ match เงื่อนไขไว้ในตัวแปรแยกต่างหาก (เช่น `voice_seg = seg` เฉพาะตอน if เข้าเงื่อนไข) ไม่ใช้ตัวแปร loop เดิมอ้างอิงต่อนอกลูป**

**วันที่:** 2026-08-10

## บทเรียนคลิป 004 (cheater-detection) — pythainlp custom_dict อาจ "แก้ไม่จริง" ถ้าคำที่หลุดเองก็อยู่ใน thai_words() อยู่แล้ว

เจอปัญหาเดิม ("naive tokenizer ตัดกลางชื่อคน") แต่คราวนี้เป็นคำไทยธรรมดา ไม่ใช่ชื่อทับศัพท์: "ประโยคแรกว่า" ถูกตัดเป็น "แร" + "กว่า" แทนที่จะเป็น "แรก" + "ว่า" — ลองเพิ่ม `'แรก'` เข้า `custom_dict` แล้ว **ยังตัดผิดเหมือนเดิม** เพราะ `'แร'` (เศษพยางค์ที่ไม่ควรเป็นคำ) **ก็ดันมีอยู่ใน `pythainlp.corpus.thai_words()` เป็นคำจริงอยู่แล้ว** ทำให้ newmm มี 2 เส้นทางตัดคำที่จำนวน token เท่ากัน (`แรก`+`ว่า` vs `แร`+`กว่า` ล้วนเป็น 2 token) แล้วเลือกเส้นทางผิดโดยไม่มีการเตือน

**ทางแก้ที่ได้ผลจริง:** ไม่ใช่แค่ "เพิ่มคำที่ต้องการเข้า custom_dict" แต่ต้อง **ลบคำที่ก่อปัญหาออกจาก dictionary ด้วย** เมื่อพบว่ามันเป็นสาเหตุของการตัดผิด:
```python
custom_dict = (set(thai_words()) - {'แร'}) | custom_names
```
**วิธีตรวจจับปัญหานี้ก่อนส่งมอบ:** อย่าเชื่อว่าเพิ่มชื่อเข้า custom_dict แล้วจบ ต้อง **รัน tokenizer จริงบนสคริปต์เต็มแล้วเช็คว่า token สั้น (≤2 ตัวอักษร) ทุกตัวเป็นคำที่สมเหตุสมผลจริง** (เทียบกับ whitelist คำฟังก์ชันสั้นที่รู้จัก) — เจอคำแปลกก็เปิดดู context รอบ ๆ ทันที (`sf.find(word)` แล้วพิมพ์ 20 ตัวอักษรก่อน-หลัง) ถ้าเจอว่าเป็นการตัดกลางคำจริง ให้ไล่หาว่า "เศษที่หลุดออกมา" (เช่น "แร") อยู่ใน `thai_words()` เองหรือไม่ ถ้าอยู่ ต้องลบออกจาก custom_dict ไม่ใช่แค่เพิ่มคำที่ถูกต้องเข้าไปเฉย ๆ

## บทเรียนคลิป 004 — anchor matching แม่นระดับ 0.00–0.30 วินาทีได้จริงด้วย normalize เลข + difflib + segment-boundary anchors

สคริปต์คลิปนี้สะกดเลขเป็นคำไทยล้วน (ไม่มีเลขอารบิกเลย) แต่ SRT จาก ASR ถอดเป็นเลขอารบิกและทับศัพท์อังกฤษปนกัน (เช่น "Cosmides และ John Tooby", "65-80%") — normalize เลขอารบิกใน SRT เป็นคำไทยด้วย `pythainlp.util.num_to_thaiword` ก่อนแล้วค่อย diff ด้วย `difflib.SequenceMatcher(autojunk=False)` ได้ coverage 96% (6810/7091 ตัวอักษร) จาก 91 matching blocks → ขยายเป็น 323 anchor points ด้วยการแทรก anchor ที่ทุกจุดข้าม SRT segment boundary ภายใน block เดียวกัน (ไม่ใช่แค่หัว-ท้าย block)

**ผลตรวจสอบจริงกับชื่อเฉพาะ 5 จุด** (เทียบเวลาที่คำนวณกับเวลาเริ่มของ SRT segment ที่ ASR ถอดชื่อนั้นมาได้จริง แม้สะกดผิดก็ยังเทียบตำแหน่งได้): "กุสตาฟ คุห์น" คลาด 0.00s · "คอสไมดีส และจอห์น ทูบี้" คลาด 0.30s · "แดเนียล ไซมอนส์ และแดเนียล เลวิน" คลาด 0.00s · ตัวเลข "65-80%" สองจุดคลาด 0.00s ทั้งคู่ (ตกอยู่ในขอบเขต SRT segment เดียวกันพอดี) — ยืนยันว่าเทคนิค normalize เลข + difflib + segment-boundary anchor ให้ความแม่นยำสูงกว่าเกณฑ์ 1.5 วิมาก แม้สคริปต์จะไม่มีเลขอารบิกเลยสักตัวก็ตาม

## บทเรียนคลิป 004 — แบ่งช็อตด้วย "เวลาเป้าหมายจริง" (ผ่านฟังก์ชัน anchor) แทนจำนวนตัวอักษรล้วน ๆ ได้จำนวนช็อตตรงเป้าเป๊ะ

แทนที่จะแบ่งช็อตด้วยจำนวนตัวอักษร/12.3 (ประมาณความเร็วอ่านเฉลี่ยทั้งคลิป) ให้แบ่งด้วย **เวลาจริงที่คำนวณจากฟังก์ชัน anchor ของแต่ละ clause** (ตัดตามช่องว่างในสคริปต์ต้นฉบับ = ขอบเขตอนุประโยคธรรมชาติที่ผู้เขียนวางไว้เอง) แล้วเมื่อ clause ยาวเกิน SPLIT_THRESHOLD ค่อยแบ่งย่อยโดยเลือกจุดตัดจาก token boundary (pythainlp) ที่ให้เวลาใกล้เคียงเป้าหมายที่สุด (ไม่ใช่ตำแหน่งตัวอักษรกึ่งกลาง) — วิธีนี้เกาะกับจังหวะพูดจริงของแต่ละประโยคแทนความเร็วเฉลี่ยทั้งคลิป

**พารามิเตอร์ที่ทดลองแล้วได้จำนวนช็อตตรงเป้าเป๊ะ:** SPLIT_THRESHOLD (ความยาว clause ที่ยังไม่ต้องแบ่ง) = 3.8s, TARGET (ความยาวย่อยเมื่อต้องแบ่ง) = 2.7s, MERGE_MIN (รวม chunk สั้นกว่านี้เข้ากับข้างเคียง) = 1.6s, MERGE_MAX (เพดานตอนรวม) = 4.2s — คลิป 548.30 วิ (เป้า 211 ช็อตจากสูตร ÷2.6) ได้ผลลัพธ์ 211 ช็อตพอดี (min 1.74s / max 4.0s / เฉลี่ย 2.60s) ไม่ต้องรันลูป "เกลี่ยความยาว 400 รอบ" แยกต่างหากอีก เพราะการแบ่ง+รวมด้วยเวลาจริงตั้งแต่ต้นให้ผลอยู่ในกรอบ 1.6–4.2 ทันที

**ข้อควรระวัง:** clause สั้นมาก (เช่นชื่อคนที่ถูกแยกออกมาเดี่ยว ๆ ตอนแบ่งยาว ๆ ด้วยสัดส่วนเวลา) ต้องมี merge-pass รวมเข้ากับเพื่อนบ้านที่สั้นกว่าเสมอ (เทียบ duration ของข้างเคียงทั้งสองฝั่งแล้วเลือกฝั่งที่สั้นกว่ามารวมก่อน) ไม่งั้นจะได้ช็อตเดี่ยว ๆ ยาวแค่ 0.08–0.9 วิ (พบ 17 จุดก่อน merge, 0 จุดหลัง merge)

**วันที่:** 2026-08-15

## บทเรียนคลิป 004 (cheater-detection) — ประกอบ CapCut โคลนจาก `003_giraffe_neck` สำเร็จ ยืนยัน pattern เดิมทั้งหมด + เจอจุดใหม่ 3 จุด

### ยืนยันซ้ำ (ไม่เปลี่ยน แต่ทดสอบผ่านจริงอีกครั้ง)
- `Timelines/<UUID>/draft_info.json` กับ root `draft_info.json` ต้อง identical เป๊ะ — เขียนทับด้วย object เดียวกันได้เลย (`diff` เปล่า 100%)
- **Timeline UUID ใช้ค่าเดิมซ้ำข้ามโปรเจกต์ได้จริง** — ตรวจพบว่า `002_first_night_sleep` และ `003_giraffe_neck` ใช้ folder UUID เดียวกันทุกตัวอักษร (`B4D1093F-64CD-409D-9C15-48C96BD6E923`) ทั้งที่เป็นคนละโปรเจกต์กัน แปลว่า **ไม่ต้อง generate UUID ใหม่สำหรับ `Timelines/<UUID>/` เวลาโคลนโปรเจกต์** — สิ่งที่ต้องเป็นค่าใหม่จริง ๆ คือ `draft_meta_info.json` → `draft_id` (คนละ field คนละความหมายกับ Timeline UUID) เพราะ `root_meta_info.json` เก็บรายการโปรเจกต์โดย key `draft_id` + `draft_fold_path` ไม่ใช่ Timeline UUID
- ภาพนิ่ง (`type: photo`) `source_timerange == target_timerange` เสมอ, `speed == 1.0` เสมอแม้คลิปเร่ง 1.10x ก็ตาม — การเร่งความเร็วคลิปทำผ่านการหารเวลา target_timerange ด้วยตัวคูณเอง ไม่แตะ field speed ของภาพ
- เสียงพากย์เร่งด้วยไฟล์นอก CapCut (`ffmpeg atempo=1.1`) แล้ว import เป็น material ใหม่ ตั้ง `segment.speed = 1.0` — ยืนยันด้วย `ffmpeg loudnorm print_format=summary` ว่า atempo ไม่ทำให้ loudness/peak เพี้ยนจากไฟล์ต้นทาง (input -14.9 LUFS/-1.2dBTP ตรงกับค่าที่มาสเตอร์ไว้ก่อนเร่ง)
- `extra_material_refs` แยกตาม track type ตายตัว: video 8 หมวด (`speeds, placeholder_infos, canvases, material_animations, sound_channel_mappings, material_colors, loudnesses, vocal_separations`), audio 6 หมวด (`speeds, placeholder_infos, beats, sound_channel_mappings, loudnesses, vocal_separations`) — โคลนจาก template แล้วสุ่ม id ใหม่ทุกตัว ห้าม reuse ข้าม segment
- BGM สอง segment ชี้ material เดียวกัน คนละ `source_timerange.start`/`volume` ต่อเนื่องไม่มีรอยตัด — segment แรกคลุมความยาวเสียงพากย์ (vol 0.10), segment สองคลุม 12 วิ end credit (vol 0.40) เริ่มที่จุดที่เสียงพากย์จบพอดี
- Ken Burns: มีแค่ `KFTypeScaleX` ใน `common_keyframes` (ไม่มี Y คู่กัน), 3 keyframe (1.0 ที่ 0%, 1.0 ที่ ~52%, 1.18 ที่ 52%+0.1s)
- `loudnesses[].enable == false` และ `vocal_separations[].choice == 0` ทุกตัวในแม่แบบ = ค่าปิดที่กฎช่องต้องการอยู่แล้ว (ห้าม Normalize/Noise Reduction/Voice Enhance)

### จุดใหม่ที่เพิ่งเจอ/ยืนยันรอบนี้

**1. ต้องปิด "ช่องว่างจากการปัดเศษ" ระหว่างช็อตสุดท้ายกับ end credit ด้วยมือ ไม่ใช่แค่หารแต่ละช็อตแยกกัน**
คำนวณ `target_duration` ของแต่ละช็อตด้วย `round(natural_dur_us / 1.10)` แบบสะสม (`target_start_i = target_end_{i-1}`) แล้วเทียบผลรวมกับความยาวไฟล์เสียง 110x จริง (วัดด้วย `wave` module) พบว่าต่างกัน **31,036 ไมโครวินาที (31ms)** เพราะ `ffmpeg atempo` ไม่ได้หารเวลาด้วย 1.10 แบบคณิตศาสตร์ล้วน ๆ (ประมวลผลเป็นช่วง ๆ ทำให้ความยาวจริงคลาดจากการหารตรง ๆ เล็กน้อย) — ถ้าปล่อยไว้จะเกิด **ช่องว่าง (gap) ในวิดีโอแทร็ก 31ms ก่อนการ์ด end credit ใบแรก** (จอจะว่าง/ดำสั้น ๆ) ตรวจพบด้วยสคริปต์เช็ค contiguity (`target_start` ของ segment ถัดไปต้อง `==` `target_start+target_duration` ของ segment ก่อนหน้าเป๊ะทุกคู่)
**ทางแก้:** หลังคำนวณ cumulative แล้ว ให้เอา drift (`voice_scaled_duration_us - sum(scaled_durations)`) ไปบวกเข้ากับ `target_duration` ของ**ช็อตสุดท้ายเท่านั้น** (ไม่กระจายคืนทุกช็อต เพราะ diff เล็กมากจนไม่กระทบ MIN/MAX bound) แล้ว assert contiguity = 0 gap ก่อนเขียนไฟล์จริงเสมอ — เคสนี้ 31ms ทำให้ช็อตสุดท้ายยาวขึ้นจาก 2.605s เป็น 2.636s ยังอยู่ในกรอบ MIN/MAX สบาย ๆ

**2. `clip.scale.x/y` ของทุก segment ภาพในแม่แบบเป็นค่าคงที่ประหลาด `0.9953809982227504` ไม่ใช่ `1.0`**
ตรวจสอบทุก segment ของ `003_giraffe_neck` (`jq '.tracks[]|select(.type=="video")|.segments[]|.clip.scale.x] | unique'`) ได้ค่าเดียวคงที่นี้ทั้งโปรเจกต์ ไม่ใช่ 1.0 ตามที่คาดไว้ตอนแรก — เป็นค่าที่หลงเหลือจากการปรับ fit ใน CapCut UI ตอนทำ 003 (ไม่ทราบที่มาแน่ชัด) ที่ Ken Burns keyframe (`KFTypeScaleX`) ใช้ค่าเริ่มต้น 1.0 (ไม่ใช่ 0.99538) แสดงว่า **เมื่อมี keyframe แล้ว engine อ่านค่าซูมจาก keyframe ล้วน ๆ ไม่ผสมกับ `clip.scale`** — คลิป 004 นี้ **คัดลอกค่า `0.9953809982227504` เดิมมาใช้กับทุก segment ภาพเหมือนกันหมด** (ทั้งที่ไม่รู้ที่มา) เพื่อความสม่ำเสมอกับสิ่งที่เคยเปิดสำเร็จมาแล้วจริง — ยังไม่ยืนยันว่าถ้าใช้ 1.0 ตรง ๆ แทนจะมีปัญหาอะไรไหม (ยังไม่ได้ทดลอง)

**3. Volume BGM จริงในไฟล์ที่ผู้ใช้อนุมัติแล้วของ 003 ไม่ตรงกับ `decisions.md` (0.0315 ไม่ใช่ 0.1)**
ตรวจ backup ไฟล์เก่าของ 003 (`draft_info.json.bak_*`) เรียงตามเวลาพบว่า **ตอนแรกตั้ง 0.1 จริงตามเอกสาร แต่หลังจากนั้น (backup เวลา 23:17 เป็นต้นไป) ถูกเปลี่ยนเป็น 0.03148680925369263 และค้างแบบนี้จนถึงเวอร์ชันที่ผู้ใช้อนุมัติสุดท้าย** — ไม่มีบันทึกเหตุผลไว้ที่ไหนเลยว่าทำไมถึงลดจาก 10% เหลือ ~3.15% (อาจเป็นการปรับฟังลดเสียง BGM ด้วยหูตอน mix จริงในแอป ไม่ใช่ error) **คลิป 004 นี้ยังใช้ค่า 0.10/0.40 ตามที่ระบุใน `decisions.md` ตรง ๆ ตามกฎ "ห้ามจำเอาเองต้องอ่านจาก decisions.md"** เพราะเป็นค่าที่บันทึกไว้เป็นทางการ — **แต่ต้องระวัง: ถ้าคลิป 004 มิกซ์เสียงแล้วรู้สึกว่า BGM ดังกว่า 003 ที่ผู้ใช้เคยชิน ให้สงสัยจุดนี้ก่อน** และถ้าผู้ใช้ปรับ volume ใหม่ใน CapCut ตอน mix สุดท้ายอีกครั้ง ให้กลับมาอัปเดตตัวเลขจริงใน `decisions.md` ให้ตรงกับสิ่งที่ shipped จริงเสมอ (ไม่ปล่อยให้เอกสารกับของจริงเพี้ยนกันแบบที่เจอใน 003)

### กระบวนการ QC ที่ควรทำเป็นมาตรฐานทุกครั้งจากนี้ (สรุปจากสคริปต์ที่ใช้จริงรอบนี้)
1. หลังคำนวณ target_timerange ทุก segment แล้ว **assert ว่าไม่มี gap/overlap ในวิดีโอแทร็ก** (`target_start` ของตัวถัดไปต้องเท่ากับ `target_end` ของตัวก่อนหน้าเป๊ะทุกคู่ รวมจุดต่อกับ end credit ด้วย)
2. **assert ว่าทุก `material_id` และทุก `extra_material_refs` ที่ segment อ้างถึง มีอยู่จริงใน `materials.*`** และไม่มี id ซ้ำกันข้าม material ในหมวดเดียวกัน
3. **assert ว่าทุกไฟล์ที่ `materials.videos[].path` และ `materials.audios[].path` ชี้ไป มีอยู่จริงบนดิสก์** ก่อนเขียนไฟล์จริง
4. เช็คไฟล์เสียงที่เร่งความเร็วแล้วด้วย `ffmpeg loudnorm print_format=summary` เทียบกับค่ามาสเตอร์ก่อนเร่ง ว่า LUFS/true peak ไม่เพี้ยนจาก `atempo`

**วันที่:** 2026-08-16
